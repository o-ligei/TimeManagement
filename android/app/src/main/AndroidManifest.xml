<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wowtime">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 连接网络权限，用于执行云端语音能力 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 获取手机录音机使用权限，听写、识别、语义理解需要用到此权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- 读取网络信息状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 获取当前wifi状态 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 允许程序改变网络连接状态 -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" /> <!-- 读取手机信息权限 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- 读取联系人权限，上传联系人需要用到此权限 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" /> <!-- 外存储写权限，构建语法需要用到此权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 外存储读权限，构建语法需要用到此权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 配置权限，用来记录应用配置信息 -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" /> <!-- 手机定位信息，用来为语义等功能提供定位，提供更精准的服务 -->
    <!-- 定位信息是敏感信息，可通过Setting.setLocationEnable(false)关闭定位请求 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- 如需使用人脸识别，还要添加：摄像头权限，拍照需要用到 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".util.ContextUtil"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".ui.others.SpeechRecognizeActivity"/>
        <activity android:name=".ui.account.InternetFriendRequestActivity" />

        <service
            android:name=".websocket.TWebSocketClientService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".ui.others.InternetFriendListActivity" />
        <activity android:name=".ui.account.EmailActivateActivity" />
        <activity android:name=".ui.alarm.AlarmPlay" />
        <activity android:name=".ui.alarm.ClockFrequencyActivity" />
        <activity android:name=".ui.alarm.TagSettingActivity" />
        <activity android:name=".ui.games.RandomNumberGameActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".ui.account.PasswordChangeActivity" />
        <activity android:name=".ui.account.CaptchaConfirmActivity" />

        <service android:name=".ui.pomodoro.FloatingImageDisplayService" />
        <service android:name=".ui.pomodoro.FloatingImageDisplayService" />

        <activity android:name=".ui.games.BlowingGameActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->
            <!--  -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".ui.games.ShakingGameActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->
            <!--  -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".ui.others.AboutActivity" /> <!-- <activity -->
        <!-- android:name=".ScreenSaverActivity" -->
        <!-- android:configChanges="orientation|keyboardHidden|screenSize" -->
        <!-- android:label="@string/title_activity_screen_saver" -->
        <!-- android:theme="@style/FullscreenTheme"></activity> -->
        <activity android:name=".ui.games.CalculateGameActivity" />
        <activity
            android:name=".ui.others.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".ui.others.CreditDetailListActivity" /> <!-- <activity android:name=".FriendsList" /> -->
        <!-- <activity android:name=".PersonInfoDetail" /> -->
        <!-- <activity -->
        <!-- android:name=".AlarmList" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <activity
            android:name=".ui.pomodoro.GameSettingActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.alarm.RingSettingActivity" />
        <activity android:name=".ui.alarm.TaskListActivity" />
        <activity android:name=".ui.alarm.TaskSuccessActivity" />
        <activity android:name=".ui.pomodoro.WhiteListActivity" />
        <activity android:name=".ui.account.RegisterActivity" />
        <activity android:name=".ui.account.LoginActivityWithPasswordActivity" />
        <activity android:name=".ui.statistic.StatisticDayActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.alarm.ClockSettingActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.account.LoginActivityWithAuthActivity" /> <!-- <activity -->
        <!-- android:name=".MainActivity" -->
        <!-- android:launchMode="standard"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.DEFAULT" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name=".ui.others.AchievementActivity" />
        <activity android:name=".ui.statistic.StatisticWeekActivity" />
        <activity android:name=".ui.statistic.StatisticYearActivity" />
        <activity
            android:name=".ui.pomodoro.PomodoroSettingActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <meta-data
            android:name="IFLYTEK_APPKEY"
            android:value="'5f9fbccc'" />
        <meta-data
            android:name="IFLYTEK_CHANNEL"
            android:value="Android_Demo" />
    </application>

</manifest>